controller:
  JCasC:
    configScripts:
      jobs: |
        jobs:
          - script: >
              pipelineJob('hello-newapp') {
                definition {
                  cpsScm {
                    scm {
                      git {
                        remote {
                          url('https://github.com/elevy99927/hello-newapp.git')
                        }
                        branches('*/lab4-solution')
                      }
                    }
                    scriptPath('Jenkinsfile')
                  }
                }
              }
