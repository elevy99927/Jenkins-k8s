controller:
  admin:
    username: "admin"
    password: "Aa123456"

  JCasC:
    configScripts:
      jobs: |
        jobs:
          - script: >
              pipelineJob('hello-newapp') {
                definition {
                  cpsScm {
                    scm {
                      git {
                        remote {
                          url('https://github.com/elevy99927/hello-newapp.git')
                        }
                        branches('*/main')
                      }
                    }
                    scriptPath('Jenkinsfile')
                  }
                }
              }
